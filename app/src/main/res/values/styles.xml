<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeWizard" parent="AppTheme">
        <item name="android:editTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
        <item name="colorControlNormal">@color/htw_orange</item>
        <item name="colorControlActivated">@color/htw_orange</item>
        <item name="colorControlHighlight">@color/htw_orange</item>
        <item name="android:spinnerStyle">@style/AppThemeWizardSpinner</item>
    </style>
    
    <style name="AppThemeWizardSpinner" parent="@android:style/Widget.Material.Light.Spinner">
        <item name="android:popupBackground">@color/primary</item>
    </style>
</resources>
